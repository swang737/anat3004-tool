function Week6Practice({ goBack }: { goBack: () => void }) {
    return (
        <div>
            <button className='bottom-back-button' onClick={goBack}>← Back</button>
        </div>
    )
}

export default Week6Practice;